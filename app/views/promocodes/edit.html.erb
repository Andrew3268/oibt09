<% if current_user && current_user.admin %>
  <div class="wrap" data-turbolinks="false">
    <h1>Editing Promocode</h1>
    <%= render 'form', promocode: @promocode %>
    <%= link_to 'Show', @promocode %> |
    <%= link_to 'Back', promocodes_path %>
  </div>
<% end %>